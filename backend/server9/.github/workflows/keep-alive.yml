name: keep-koyeb-awake

on:
  schedule:
    - cron: '*/5 * * * *'  # every 5 minutes

jobs:
  ping-koyeb:
    runs-on: ubuntu-latest
    steps:
      - name: Curl Koyeb health endpoint
        run: curl -fsS https://wiz-backend-giantwizard-0f2a46ea.koyeb.app/ || true
